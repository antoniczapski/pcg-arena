# MarioDPO_v2 dependencies
# ---------------------------------------------------------------------------
# Split into two logical groups. The "judge" group is pure-CPU scientific
# Python and is enough for Phases 1-2 (feature extraction + judge training).
# The "training" group is the heavy ML stack needed for Phases 3-7 (SFT + DPO).
#
# Pin ranges that are known to work together with transformers/trl DPOTrainer.
# Tested target: Python 3.10-3.12, CUDA 11.8/12.1 on an NVIDIA GTX 2060 (6 GB).
# ---------------------------------------------------------------------------

# --- Judge / data group (CPU only) ---
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
joblib>=1.3
PyYAML>=6.0
tqdm>=4.65

# --- Training group (GPU recommended) ---
# Install torch separately matching your CUDA build, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# The versions below are a coherent, tested set (transformers 4.57 + trl 1.x).
torch>=2.1
transformers>=4.57,<5
trl>=1.6,<2
datasets>=2.18
peft>=0.10
accelerate>=0.29
wandb>=0.16
huggingface-hub>=0.23
rich>=13.0          # transitive dep of trl callbacks; pinned explicitly
